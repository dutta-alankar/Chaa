# sedov_runtime_params.ini — canonical configuration for the 'sedov'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/sedov_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = sedov
geometry = spherical
nx1 = 512
x1min = 0
x1max = 1.2
bcX1min = reflect
sedovR0 = 0.02
tstop = 0.5
outFormats = txt
