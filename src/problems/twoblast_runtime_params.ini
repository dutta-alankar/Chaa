# twoblast_runtime_params.ini — canonical configuration for the 'twoblast'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/twoblast_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = twoblast
geometry = cartesian
nx1 = 800
x1min = 0
x1max = 1
bcX1min = reflect
bcX1max = reflect
tstop = 0.038
outFormats = txt
