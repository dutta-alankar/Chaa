# riemann2d_runtime_params.ini — canonical configuration for the 'riemann2d'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/riemann2d_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = riemann2d
geometry = cartesian
nx1 = 192
nx2 = 192
x1min = 0
x1max = 1
x2min = 0
x2max = 1
tstop = 0.3
outFormats = hdf5
