# kh_runtime_params.ini — canonical configuration for the 'kh'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/kh_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = kh
geometry = cartesian
nx1 = 128
nx2 = 128
x1min = 0
x1max = 1
x2min = 0
x2max = 1
bcX1min = periodic
bcX1max = periodic
bcX2min = periodic
bcX2max = periodic
tstop = 1.5
outFormats = hdf5
