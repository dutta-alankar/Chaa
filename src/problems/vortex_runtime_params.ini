# vortex_runtime_params.ini — canonical configuration for the 'vortex'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/vortex_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = vortex
geometry = cartesian
nx1 = 64
nx2 = 64
x1min = 0
x1max = 10
x2min = 0
x2max = 10
cen1 = 5
cen2 = 5
bcX1min = periodic
bcX1max = periodic
bcX2min = periodic
bcX2max = periodic
tstop = 10
outFormats = hdf5
