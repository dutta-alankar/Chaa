# blast_runtime_params.ini — canonical configuration for the 'blast'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/blast_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = blast
geometry = polar
nx1 = 128
nx2 = 256
x1min = 0.5
x1max = 1.5
x2min = 0
x2max = 6.28318530717959
bcX2min = periodic
bcX2max = periodic
cen1 = -1
cen2 = 0
blastR0 = 0.1
tstop = 0.15
outFormats = hdf5
