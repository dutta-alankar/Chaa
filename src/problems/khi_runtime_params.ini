# khi_runtime_params.ini — canonical configuration for the 'khi'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/khi_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = khi
geometry = cartesian
nx1 = 256
nx2 = 64
x1min = 0
x1max = 4
x2min = 0
x2max = 1
bcX1min = periodic
bcX1max = periodic
eos = isothermal
csIso = 10
tstop = 1.0
outFormats = hdf5
