# thermalWave_runtime_params.ini — canonical configuration for the 'thermalWave'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/thermalWave_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = thermalWave
geometry = cartesian
nx1 = 128
x1min = -0.5
x1max = 0.5
bcX1min = periodic
bcX1max = periodic
kappa = 0.02
twAmp = 0.001
tstop = 2.0
outFormats = txt
