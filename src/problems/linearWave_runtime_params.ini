# linearWave_runtime_params.ini — canonical configuration for the 'linearWave'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/linearWave_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = linearWave
geometry = cartesian
nx1 = 128
x1min = 0
x1max = 1
bcX1min = periodic
bcX1max = periodic
recon = wenoz
integrator = vl2
waveAmp = 0.0001
tstop = 1.0
outFormats = txt
