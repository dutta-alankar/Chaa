# turbulence_runtime_params.ini — canonical configuration for the 'turbulence'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/turbulence_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = turbulence
geometry = cartesian
nx1 = 64
nx2 = 64
x1min = 0
x1max = 1
x2min = 0
x2max = 1
bcX1min = periodic
bcX1max = periodic
bcX2min = periodic
bcX2max = periodic
eos = isothermal
csIso = 1
forceAmp = 2.0
forceTcorr = 0.5
forceKmin = 1
forceKmax = 2
tstop = 3
outFormats = hdf5
