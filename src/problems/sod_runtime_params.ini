# sod_runtime_params.ini — canonical configuration for the 'sod'
# test problem.  Run with:
#   ./build/bin/chaa --paramsFile=src/problems/sod_runtime_params.ini
# Any key can still be overridden on the command line (--key=value).

problem = sod
geometry = cartesian
nx1 = 400
x1min = 0
x1max = 1
tstop = 0.2
outFormats = txt,hdf5
